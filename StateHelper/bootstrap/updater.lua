--[[
Open with encoding: UTF-8
StateHelper/bootstrap/updater.lua

Compatibility shim for legacy runtime code that still requires
`StateHelper.bootstrap.updater`.
]]

return require('StateHelper.Arizona_bootstrap.updater')
